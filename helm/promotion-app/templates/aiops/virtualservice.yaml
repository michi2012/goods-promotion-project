apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.aiops.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.aiops.name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  hosts:
    - {{ .Values.aiops.name }}
  http:
    - route:
        - destination:
            host: {{ .Values.aiops.name }}
            subset: v1
          weight: 100
        - destination:
            host: {{ .Values.aiops.name }}
            subset: v2
          weight: 0
