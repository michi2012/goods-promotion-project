apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.gateway.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/name: {{ .Values.gateway.name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  hosts:
    - {{ .Values.gateway.name }}
  http:
    - route:
        - destination:
            host: {{ .Values.gateway.name }}
            subset: v1
          weight: 100
        - destination:
            host: {{ .Values.gateway.name }}
            subset: v2
          weight: 0
